server:
  port: 8080

spring:
  application:
    name: pipeline-builder

  datasource:
    url: jdbc:mysql://rm-cn-v3m4jkbul00025to.rwlb.rds.aliyuncs.com:3306/fish?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: fish
    password: 32KjyZ_uW9L9_Q5
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

logging:
  level:
    com.pipeline: DEBUG
    org.hibernate.SQL: DEBUG
